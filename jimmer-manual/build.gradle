version = '0.2.0'
description = 'manually construct Jimmer-Module-System at runtime'

dependencies {
    compileOnly platform(libs.jimmer.bom)

    compileOnly 'org.babyfish.jimmer:jimmer-sql'
    compileOnly libs.jspecify
    compileOnly libs.slf4j.api
}
